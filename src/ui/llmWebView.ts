export class LlmWebView {
  private panel?: any;
  constructor(private context: any) {}
  public openUI(): void {
    // Minimal stub for now
  }
  public streamOutput(_text: string): void {
    // Stub
  }
}
